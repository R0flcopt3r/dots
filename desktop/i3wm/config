# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Variables
set $mod Mod4
set $TERMINAL /usr/bin/alacritty
set $launcher rofi -show run

## I3wm Settings
font pango:DejaVu Sans Mono 8
floating_modifier $mod
workspace_auto_back_and_forth yes
focus_follows_mouse no
force_display_urgency_hint 500 ms

# Dropdown/Scratchpad Windows
#I'll later bind this to mod+u.
for_window [instance="dropdown"] floating enable
for_window [instance="dropdown"] resize set 625 400
for_window [instance="dropdown"] move position center
for_window [instance="dropdown"] move scratchpad
exec --no-startup-id $TERMINAL --class dropdown

# Python window
for_window [instance="python"] floating enable
for_window [instance="python"] resize set 625 400
for_window [instance="python"] move position center
for_window [instance="python"] move scratchpad
exec --no-startup-id $TERMINAL --class python -e "ipython3"

# Qutebrowser
for_window [class="qutebrowser"] layout tabbed
assign [class="qutebrowser"] 1:browser

# Steam
no_focus [class="Steam"]
assign [class="Steam"] 8

# Discord
no_focus [instance="discord"]
assign [instance="discord"] 2:COM

# Spotify
no_focus [instance="spotify"]
assign [instance="spotify"] 2:COM

no_focus [class="zoom"]
assign [class="zoom"] 6

no_focus [class="microsoft teams - preview"]
assign [class="microsoft teams - preview"] 6
for_window [title="Microsoft Teams Notification"] floating enable
for_window [title="Microsoft Teams Notification"] resize set 300 200

for_window [class="Zathura"] floating enable
for_window [class="Zathura"] resize set 637 900

for_window [class="Matplotlib"] floating enable
#for_window [class="Matplotlib"] resize set 1920 700
for_window [class="Matplotlib"] move position center


## Bindings
bindsym $mod+q          exec xautolock -locknow
bindsym $mod+Shift+q    kill

bindsym $mod+w          layout tabbed

bindsym $mod+e          layout toggle split
bindsym $mod+Shift+e\
        exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

bindsym $mod+r          mode "resize"
bindsym $mod+Shift+r    restart

bindsym $mod+t          exec surf

bindsym $mod+y          move workspace to output up

bindsym $mod+u          exec surf my.1password.com

bindsym $mod+o          split h

bindsym $mod+p          exec /home/eirik/.config/scripts/setmon

bindsym $mod+a          focus parent

bindsym $mod+s          layout stacking

bindsym $mod+d          exec $launcher

bindsym $mod+f          fullscreen

bindsym $mod+h          focus left
bindsym $mod+Shift+h    move left
bindsym $mod+Control+h  focus parent; focus left

bindsym $mod+j                focus down
bindsym $mod+Shift+j          move down
bindsym $mod+Control+j        focus parent; focus down
bindsym $mod+Shift+Control+j  exec /home/eirik/.screenlayout/reotate-left.sh

bindsym $mod+k          focus up
bindsym $mod+Shift+k    move up
bindsym $mod+Control+k  focus parent; focus up

bindsym $mod+l          focus right
bindsym $mod+Shift+l    move right
bindsym $mod+Control+l  focus parent; focus right

bindsym $mod+z			exec amixer sset Capture toggle && \
							 pkill -SIGRTMIN+10 i3blocks

bindsym $mod+x          move workspace to output left

bindsym $mod+c          [instance="python"] scratchpad show;\
                        move position center

bindsym $mod+v          split v

bindsym $mod+b          sticky toggle

bindsym $mod+m          [instance="dropdown"] scratchpad show;\
                        move position center

bindsym $mod+Return            exec $TERMINAL
bindsym $mod+Shift+Return exec $HOME/Documents/Script/using/newShell_WD.sh

bindsym $mod+Shift+space       floating toggle

bindsym $mod+space focus mode_toggle

# Mouse buttons

# Arrow keys
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

bindsym $mod+Control+Left focus parent; focus left
bindsym $mod+Control+Right focus parent; focus right
bindsym $mod+Control+Down focus parent; focus down
bindsym $mod+Control+up focus parent; focus up

# switch to workspace
bindsym $mod+1 workspace 1:Browser
bindsym $mod+2 workspace 2:COM
bindsym $mod+3 workspace 3:CLI
bindsym $mod+4 workspace 4:CLI
bindsym $mod+5 workspace 5:vBOX
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10
bindsym $mod+Tab workspace back_and_forth
bindsym $mod+BackSpace workspace back_and_forth

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1:Browser
bindsym $mod+Shift+2 move container to workspace 2:COM
bindsym $mod+Shift+3 move container to workspace 3:CLI
bindsym $mod+Shift+4 move container to workspace 4:CLI
bindsym $mod+Shift+5 move container to workspace 5:vBOX
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10


# resize window (you can also use the mouse for that)
mode "resize" {
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt
        bindsym Left resize shrink width 1 px or 1 ppt
        bindsym Down resize grow height 1 px or 1 ppt
        bindsym Up resize shrink height 1 px or 1 ppt
        bindsym Right resize grow width 1 px or 1 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

## Function Keys
bindsym XF86AudioRaiseVolume            exec $HOME/Git/dots/scripts/vol.sh -v inc
bindsym XF86AudioLowerVolume            exec $HOME/Git/dots/scripts/vol.sh -v dec
bindsym Shift+XF86AudioRaiseVolume      exec $HOME/Git/dots/scripts/vol.sh -v inc
bindsym Shift+XF86AudioLowerVolume      exec $HOME/Git/dots/scripts/vol.sh -v dec
bindsym XF86AudioMute                   exec $HOME/Git/dots/scripts/vol.sh -v toggle
bindsym XF86AudioMicMute                exec amixer -q -D sset Capture toggle
bindsym XF86MonBrightnessDown           exec xbacklight -dec 10 && pkill -SIGRTMIN+11 i3blocks
bindsym XF86MonBrightnessUp             exec xbacklight -inc 10 && pkill -SIGRTMIN+11 i3blocks
bindsym Shift+XF86MonBrightnessDown     exec xbacklight -dec 1 && pkill -SIGRTMIN+11 i3blocks
bindsym Shift+XF86MonBrightnessUp       exec xbacklight -inc 1 && pkill -SIGRTMIN+11 i3blocks
bindsym XF86WLAN                        exec /home/eirik/Documents/Script/using/wifi-toggle
bindsym XF86AudioPlay                   exec playerctl play-pause
bindsym XF86AudioNext                   exec playerctl next
bindsym XF86AudioPrev                   exec playerctl previous
bindsym XF86Calculator                  exec qalculate-gtk
bindsym Print                           exec scrot '%Y-%m-%d_$wx$h.png' -s -e 'mv $f ~/Pictures/'

set_from_resource $foreground URxvt.foreground
set_from_resource $background URxvt.background
set_from_resource $cursorColor URxvt.cursorColor
set_from_resource $black       URxvt.color0 #000000
set_from_resource $red         URxvt.color1 #ff0000
set_from_resource $green       URxvt.color2 #00ff00
set_from_resource $yellow      URxvt.color3 #ffff00
set_from_resource $blue        URxvt.color4 #0000ff
set_from_resource $magenta     URxvt.color5 #ff00ff
set_from_resource $cyan        URxvt.color6 #00ffff
set_from_resource $white       URxvt.color7 #ffffff

## window colors
##                       border                 background          text            indicator
client.focused           $cursorColor        $cursorColor        $background         $cyan
client.unfocused         $background         $white              $foreground         $white
client.focused_inactive  $background         $white              $foreground         $white
client.urgent            $red                $red                $foreground         $cyan


# bar
bar {
    tray_output primary
    status_command i3blocks
    colors {
        background $background
            separator #757575
        #                  border       background      text
        focused_workspace  $cursorColor     $cursorColor    $background
        inactive_workspace $background      $background     $foreground
        urgent_workspace   $red             $red            $foreground
    }
}

# I3 gaps
for_window [class="^.*"] border pixel 2
gaps inner 10
gaps outer 10
smart_gaps on

# Disable gaps for certain workspace
workspace 1 gaps inner 0
workspace 1 gaps outer 0

set $mode_gaps Gaps: (o) outer, (i) inner, (a) all
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_all All Gaps: +|-|0 (local), Shift + +|-|0 (global)
bindsym $mod+Shift+g mode "$mode_gaps"

mode "$mode_gaps" {
        bindsym o      mode "$mode_gaps_outer"
        bindsym i      mode "$mode_gaps_inner"
        bindsym a      mode "$mode_gaps_all"
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

mode "$mode_gaps_inner" {
        bindsym plus  gaps inner current plus 5
        bindsym minus gaps inner current minus 5
        bindsym 0     gaps inner current set 0

        bindsym Shift+plus  gaps inner all plus 5
        bindsym Shift+minus gaps inner all minus 5
        bindsym Shift+0     gaps inner all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_outer" {
        bindsym plus  gaps outer current plus 5
        bindsym minus gaps outer current minus 5
        bindsym 0     gaps outer current set 0

        bindsym Shift+plus  gaps outer all plus 5
        bindsym Shift+minus gaps outer all minus 5
        bindsym Shift+0     gaps outer all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}
mode "$mode_gaps_all" {
     bindsym plus gaps outer current plus 5; gaps inner current plus 5
     bindsym minus gaps outer current minus 5; gaps inner current minus 5
     bindsym Shift+minus gaps outer all minus 5; gaps inner current minus 5
     bindsym Shift+plus gaps outer all plus 5; gaps inner current plus 5
     bindsym 0 gaps outer current set 0; gaps inner current set 0
     bindsym Shift+0 gaps outer all set 0; gaps inner current set 0
     bindsym Return mode "default"
     bindsym Escape mode "default"
}


# floating
# qalculator
for_window [title="Qalculate!"] floating enable
for_window [title="Qalculate!"] resize set 300 425

# xauth
for_window [title="OpenSSH*"] floating enable

# PavuControl
for_window [title="Volume*"] floating enable
for_window [title="Volume*"] resize set 800 500

# Blueman-manager
for_window [title="Bluetooth*"] floating enable
for_window [title="Bluetooth*"] resize set 400 800

# PDFpc
for_window [ title="^pdfpc - present" ] border none floating enable

# Origin
for_window [instance="origin.exe"] floating enable

## Startup programs
exec --no-startup-id blueman-applet
exec --no-startup-id nextcloud
exec --no-startup-id /usr/libexec/notification-daemon
exec --no-startup-id flameshot
exec_always --no-startup-id feh --bg-fill $HOME/Pics/Wallpapers-laptop/wallpaper-current.jpg
